name: CI/CD Pipeline1

on:
  push:
    branches:
      - pankaj

jobs:
  build-and-deploy:
    runs-on: self-hosted

    steps:
    # # Step 1: Checkout code1
    # - name: Checkout code
    #   uses: actions/checkout@v2

    # # Step 2: Log in to Docker Hub
    # - name: Log in to Docker Hub
    #   uses: docker/login-action@v2
    #   with:
    #     username: ${{ secrets.DOCKER_USERNAME }}
    #     password: ${{ secrets.DOCKER_PASSWORD }}

    # # Step 3: Build and push Docker image
    # - name: Build and Push Docker Image
    #   run: |
    #     docker build -t ${{ secrets.DOCKER_USERNAME }}/flask-app:latest .
    #     docker push ${{ secrets.DOCKER_USERNAME }}/flask-app:latest

    # # Step 4: Set up Kubernetes CLI
    # - name: Set up kubectl
    #   uses: azure/setup-kubectl@v3
    #   with:
    #     version: 'latest'

    # # Step 5: Configure kubeconfig
    # - name: Configure kubeconfig
    #   run: |
    #     echo "${{ secrets.KUBE_CONFIG }}" > kubeconfig
    #     export KUBECONFIG=$PWD/kubeconfig

    # Step 6: Deploy with Helm
    - name: Deploy with Helm
      run: |
        helm upgrade --install flask-app /home/ubuntu/ansibletest/app/flask-app \
          --set image.repository=${{ secrets.DOCKER_USERNAME }}/python25 \
          --set image.tag=jan2
